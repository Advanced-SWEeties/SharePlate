import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f5f5f5', // Light gray
      paper: '#ffffff',   // Background for cards or components
    },
  },
});

export default theme;
